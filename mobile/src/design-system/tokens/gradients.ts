import { gradients as foundationGradients } from '../foundation/gradients';

export const gradients = {
  primary: foundationGradients.orange,
  secondary: foundationGradients.orangeSoft,
  background: foundationGradients.creamBg,
  premium: foundationGradients.premium,
};
