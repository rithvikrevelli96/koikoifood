import { radius as foundationRadius } from '../foundation/radius';

export const radius = {
  control: foundationRadius.lg,   // Inputs & Buttons (18px)
  card: foundationRadius.xl,      // Cards (24px)
  dialog: foundationRadius.xxl,   // Dialogs (28px)
  sheet: foundationRadius.huge,   // Bottom Sheets (32px)
  max: foundationRadius.max,
};
