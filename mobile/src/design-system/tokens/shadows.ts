import { shadows as foundationShadows } from '../foundation/shadows';

export const shadows = {
  card: foundationShadows.soft,
  dialog: foundationShadows.dialog,
  floating: foundationShadows.medium,
};
