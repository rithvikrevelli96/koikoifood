import { spacing as foundationSpacing } from '../foundation/spacing';

export const spacing = {
  xs: foundationSpacing.xs,
  sm: foundationSpacing.sm,
  md: foundationSpacing.md,
  lg: foundationSpacing.lg,
  xl: foundationSpacing.xl,
  xxl: foundationSpacing.xxl,
  outer: foundationSpacing.outer,
};
