import { SplashFeatureFlags } from '../../types/splash';

export const SPLASH_FLAGS: SplashFeatureFlags = {
  enableLandscape: true,
  enableScooter: true,
  enableCTA: false,
  enableFeatureCards: true,
  enableDecorations: true,
  enableHeroAnimation: true,
};
